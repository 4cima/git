---
inclusion: auto
description: "قواعد صارمة للتعامل مع Turso - لا تُنتهك"
priority: critical
---

# 🔴 TURSO RULES - ABSOLUTE

**تاريخ الإنشاء:** 2026-04-28  
**الأولوية:** حرجة - لا تُنتهك أبداً

---

## ⛔ القاعدة الذهبية

**لا تلمس Turso إلا بإذن صريح ومباشر من المستخدم!**

---

## 🚫 ممنوع منعاً باتاً

### ❌ لا تفعل هذا أبداً:
1. ❌ تطبيق Schema على Turso تلقائياً
2. ❌ إضافة بيانات إلى Turso تلقائياً
3. ❌ تشغيل سكريبت مزامنة بدون أمر صريح
4. ❌ حذف بيانات من Turso بدون أمر صريح
5. ❌ تعديل أي شيء في Turso بدون إذن مباشر
6. ❌ إنشاء جداول في Turso تلقائياً
7. ❌ إعادة تعيين Turso بدون أمر واضح

---

## ✅ متى يُسمح بلمس Turso؟

### فقط في حالتين:

#### 1. عند تشغيل سكريبت المزامنة
```bash
# المستخدم يكتب بنفسه:
node scripts/sync-to-turso-optimized.js --priority=1
```

#### 2. بأمر واضح وصريح
```
المستخدم يقول:
- "طبق الـ schema على Turso"
- "أضف البيانات الثابتة لـ Turso"
- "ازامن مع Turso"
- "احذف بيانات Turso"
```

---

## 📋 سير العمل الصحيح

### ✅ الطريقة الصحيحة:

```
1. المستخدم: "اسحب أفلام من TMDB"
   → تنفيذ: السحب إلى SQLite المحلية ✅

2. المستخدم: "عالج البيانات"
   → تنفيذ: المعالجة على SQLite المحلية ✅

3. المستخدم: "ترجم العناوين"
   → تنفيذ: الترجمة على SQLite المحلية ✅

4. المستخدم: "ازامن مع Turso"
   → تنفيذ: المزامنة من SQLite إلى Turso ✅
```

### ❌ الطريقة الخاطئة:

```
1. المستخدم: "اسحب أفلام من TMDB"
   → ❌ لا تضيفها لـ Turso تلقائياً!
   → ✅ أضفها لـ SQLite المحلية فقط

2. المستخدم: "أضف الأنواع"
   → ❌ لا تضيفها لـ Turso تلقائياً!
   → ✅ أضفها لـ SQLite المحلية فقط
```

---

## 🎯 القاعدة المحلية vs Turso

### SQLite المحلية (data/4cima-local.db):
- ✅ **كل العمل يتم عليها**
- ✅ السحب من TMDB
- ✅ المعالجة والترجمة
- ✅ الفلترة والتنظيف
- ✅ التطوير والاختبار
- ✅ **بدون إذن - اشتغل براحتك**

### Turso (Production):
- ⛔ **للإنتاج فقط**
- ⛔ لا نلمسها إلا بإذن صريح
- ⛔ المزامنة فقط عند الطلب
- ⛔ **بإذن فقط - لا تلمسها!**

---

## 📝 أمثلة عملية

### ✅ صحيح:

**المستخدم:** "اسحب 1000 فيلم"
```javascript
// ✅ صحيح
const db = require('./services/local-db'); // SQLite المحلية
// السحب والإضافة للقاعدة المحلية
```

**المستخدم:** "الآن ازامن مع Turso"
```javascript
// ✅ صحيح - بأمر صريح
node scripts/sync-to-turso-optimized.js
```

### ❌ خاطئ:

**المستخدم:** "اسحب 1000 فيلم"
```javascript
// ❌ خاطئ - لا تفعل هذا!
const turso = require('./services/turso-client');
// لا تضيف لـ Turso مباشرة!
```

---

## 🚨 إذا أخطأت

إذا لمست Turso بدون إذن:
1. اعتذر فوراً
2. اشرح ما فعلته بالضبط
3. اسأل: هل تريد التراجع؟
4. لا تكرر الخطأ

---

## 💡 تذكر دائماً

> **"القاعدة المحلية = كل العمل"**  
> **"Turso = بإذن فقط"**

---

**هذا الملف يُقرأ في كل محادثة جديدة**  
**لا تنسى هذه القواعد أبداً**
