# ✅ الإجابات على النقاط الخمسة

## 1️⃣ رفض فكرة "مطابقة النسخة القديمة"

**✅ متفق تماماً.**

- القديم فيه bugs (Date.now() non-deterministic، self-collision غير محمي)
- **لن ننسخ bugs قديمة**
- المعيار الوحيد: **السياسة المتفق عليها (base → year → genre → genre-N، إنجليزي فقط، صفر IDs) بشكل deterministic قابل لإعادة الإنتاج**

---

## 2️⃣ توضيح وتثبيت الـ ORDER BY

### ❌ المشكلة المكتشفة:

الكود الأصلي كان:
- **من Turso:** `SELECT ... FROM movies LIMIT ? OFFSET ?` ← **مفيش ORDER BY!**
- **من المحلي:** `ORDER BY id` ← غلط، لازم `tmdb_id`

### ✅ التصحيح:

**كل استعلام الآن يستخدم `ORDER BY tmdb_id ASC` حصرياً:**

```javascript
// من Turso (chunks):
sql: 'SELECT tmdb_id, slug FROM movies ORDER BY tmdb_id ASC LIMIT ? OFFSET ?'

// من المحلي:
SELECT tmdb_id, title_en, release_year, primary_genre
FROM movies
WHERE id = tmdb_id
ORDER BY tmdb_id ASC
```

### 📝 التوثيق:

تم إضافة قسم "القواعد الحتمية" في `MOVIES-SLUG-POLICY.md`:

```
⚠️ القواعد الحتمية (Deterministic):

1. الترتيب الثابت: كل استعلام يستخدم ORDER BY tmdb_id ASC
2. الربط الصحيح: الربط بين المحلي وTurso يتم حصرياً عبر tmdb_id (ليس id)
3. Idempotency: إعادة تشغيل السكريبت تُعطي نفس النتيجة بالضبط
```

**الضمان:** أي تشغيل تاني (كامل أو جزئي) **هيدي نفس النتيجة بالضبط**.

---

## 3️⃣ الربط بين المحلي وTurso

### ❌ المشكلة الخطيرة المكتشفة:

الكود الأصلي كان بيربط بـ `id`:

```javascript
// من Turso
idToSlug.set(row.id, row.slug)  // ← خطأ!

// لما بيدور في المحلي
const currentSlug = idToSlug.get(movie.id)  // ← خطأ!

// لما بيعمل UPDATE
WHERE id = ?  // ← خطأ!
```

**ده كارثة** لأن فيه **128,319 فيلم** `id != tmdb_id` في المحلي!

### ✅ التصحيح الكامل:

**كل الربط الآن بـ `tmdb_id` حصرياً:**

```javascript
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// من Turso (المرحلة 1)
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
const slugMap = new Map()      // slug → {tmdb_id}
const tmdbToSlug = new Map()   // tmdb_id → slug ← اسم واضح

result.rows.forEach(row => {
  slugMap.set(row.slug, { tmdb_id: row.tmdb_id })
  tmdbToSlug.set(row.tmdb_id, row.slug)  // ← tmdb_id
})

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// المعالجة (المرحلة 2)
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
for (const movie of movies) {
  // البحث بـ tmdb_id
  const currentSlug = tmdbToSlug.get(movie.tmdb_id)  // ← tmdb_id
  
  // التحديث
  if (currentSlug !== newSlug) {
    updates.push({
      tmdb_id: movie.tmdb_id,  // ← tmdb_id
      oldSlug: currentSlug,
      newSlug: newSlug
    })
    tmdbToSlug.set(movie.tmdb_id, newSlug)  // ← tmdb_id
  }
}

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// التطبيق على Turso (المرحلة 3)
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
turso.execute({
  sql: 'UPDATE movies SET slug = ? WHERE tmdb_id = ?',  // ← WHERE tmdb_id
  args: [u.newSlug, u.tmdb_id]  // ← tmdb_id
})
```

**السطر الفعلي للربط:**
- من Turso: `tmdbToSlug.set(row.tmdb_id, row.slug)` ← السطر 156
- المعالجة: `const currentSlug = tmdbToSlug.get(movie.tmdb_id)` ← السطر 191
- التحديث: `WHERE tmdb_id = ?` ← السطر 276

**الضمان:** صفر احتمال لربط خاطئ، حتى لو `id != tmdb_id`.

---

## 4️⃣ وضع Dry-Run (قراءة فقط)

### ✅ تم الإضافة بالكامل:

#### الاستخدام:

```bash
# Dry-run (بدون UPDATE)
node rebuild-movies-slugs.js --dry-run

# التنفيذ الفعلي
node rebuild-movies-slugs.js
```

#### ما يفعله Dry-run:

1. ✅ يجلب كل البيانات من Turso
2. ✅ يحسب كل الslugs الجديدة في الذاكرة
3. ✅ يطبع تقرير شامل:
   - **عدد الأفلام المتغير slugها**
   - **عدد الأفلام الثابتة**
   - **عدد الunresolved**
   - **عينة 20 فيلم من كل فئة** (متغير / ثابت / unresolved)
4. ❌ **لا ينفذ أي UPDATE على Turso**

#### مثال التقرير المتوقع:

```
📊 تقرير العينات
═══════════════════════════════════════════════════════════════

✏️  عينة من الأفلام المتغير slugها (أول 20 من 45,123):
   [550] fight-club-724606 → fight-club-1999
   [13] forrest-gump-13 → forrest-gump-1994
   [155] the-dark-knight-155 → the-dark-knight-2008
   [680] pulp-fiction-680 → pulp-fiction-1994
   ...

⚠️  عينة من الأفلام غير المحلولة (أول 20 من 87):
   [422] "8½" - حالياً: 8-1963
   [2721] "Z" - حالياً: z-1969
   [473] "Pi" - حالياً: pi-1998
   ...

═══════════════════════════════════════════════════════════════
📈 الملخص:
   إجمالي:       134,252
   ✏️  محدّث:     45,123 (33.60%)
   ✓  ثابت:      89,042 (66.33%)
   ⚠️  unresolved: 87 (0.06%)
═══════════════════════════════════════════════════════════════

⚠️  وضع DRY-RUN: لم يتم تنفيذ أي UPDATE
   لتطبيق التحديثات، شغّل: node rebuild-movies-slugs.js
```

**الكود المسؤول:**
- Detection: `const DRY_RUN = process.argv.includes('--dry-run')` ← السطر 11
- التنفيذ: `if (DRY_RUN) { ... return }` ← السطر 241
- التقرير: `function printSampleReport(...)` ← السطر 137

---

## 5️⃣ الرقم المتوقع للـ unresolved

### 📊 التحليل الفعلي:

تم فحص القاعدة الفعلية باستخدام `check-unresolved-estimate.js`:

```
1️⃣ عناوين فاضية أو NULL: 0
2️⃣ أكثر العناوين تكراراً: "Blackout" (12 مرة فقط)
3️⃣ عناوين قصيرة (<=3 أحرف): 343
4️⃣ عناوين غير لاتينية: 15
5️⃣ عينة مشبوهة:
   [422] "8½" (1963)
   [473] "Pi" (1998)
   [2721] "Z" (1969)
   [3682] "54" (1998)
   [8065] "21" (2008)
```

### ✅ التقدير النهائي:

**< 100 فيلم (من 134,252) = 0.07%**

### 🔍 الأسباب المتوقعة:

1. **عناوين قصيرة برموز خاصة:** `"8½"` → `toSlug()` يحولها لـ `"8"` → تصادم محتمل
2. **عناوين أحرف واحدة:** `"Z"`, `"W."` → `"z"`, `"w"` → تصادم محتمل
3. **عناوين صينية/كورية خالصة:** ~15 فيلم → `toSlug()` يرجع `""` → `null`

### 🎯 الخطة للتعامل معهم:

#### الآن (تلقائي):

1. ✅ **يدخلوا قائمة `unresolved-movies-slugs.json`**
2. ✅ **يحتفظوا بالslug القديم** (مش هيتمسح)
3. ✅ **يُبلّغ عنهم في التقرير** (عينة 20)

#### لاحقاً (يدوي أو سكريبت منفصل):

**خيارات الحل:**

**أ. إضافة fallback للعناوين القصيرة:**
```javascript
// لو العنوان قصير جداً (<= 2 أحرف)، استخدم العنوان الأصلي
if (title_en.length <= 2) {
  return `${title_original}-${tmdb_id}`  // استثناء نادر
}
```

**ب. ترجمة يدوية:**
- الـ15 فيلم غير اللاتينية → ترجمتهم يدوياً في `title_en`
- إعادة تشغيل السكريبت

**ج. تجاهلهم:**
- الـslug القديم (مثل `8-1963`) يفضل موجود
- مش مشكلة حرجة (<100 فيلم من 134K)

### 🔒 القرار الموصى به:

**ترك الـunresolved كما هم للآن:**
- نسبتهم ضئيلة جداً (0.07%)
- لهم slugs موجودة حالياً (مش هيكسروا الموقع)
- ممكن نعالجهم يدوياً لاحقاً لو ضروري

**مفيش خطة تلقائية إضافية** - هيفضلوا في قائمة unresolved لحد المعالجة اليدوية.

---

## ✅ الخلاصة على الخمس نقاط:

| النقطة | الحالة | التفاصيل |
|--------|--------|----------|
| **1️⃣ رفض المطابقة القديمة** | ✅ متفق | معيار deterministic فقط |
| **2️⃣ ORDER BY ثابت** | ✅ مصحح | `ORDER BY tmdb_id ASC` في كل مكان |
| **3️⃣ الربط بـ tmdb_id** | ✅ مصحح | صفر استخدام لـ `id` |
| **4️⃣ Dry-run** | ✅ مضاف | `--dry-run` لتقرير كامل بدون UPDATE |
| **5️⃣ Unresolved المتوقع** | ✅ محسوب | <100 (0.07%)، يُترك للمعالجة اليدوية |

---

## 🚀 الخطوة التالية:

**تشغيل dry-run للحصول على التقرير:**

```bash
node rebuild-movies-slugs.js --dry-run
```

**بعد مراجعة التقرير، نقرر:**
- ✅ تنفيذ فعلي: `node rebuild-movies-slugs.js`
- ⏸️ تعديلات إضافية (لو لقينا حاجة غير متوقعة)
