{
  "name": "4cima-player",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone video player Worker for 4cima.stream (dedicated account)",
  "scripts": {
    "deploy": "node scripts/deploy-player.cjs",
    "deploy:check": "node scripts/deploy-player.cjs --check"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}