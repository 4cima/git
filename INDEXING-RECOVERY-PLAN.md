# خطة استعادة الفهرسة الكاملة — 4cima.com
**التاريخ:** 21 سبتمبر 2026 · **الحالة عند كتابة الخطة:** 53 صفحة مفهرسة / ~88,000 غير مفهرسة (87,941 «تم اكتشافها — لم تُفهرس»)

---

## الفهم المبدئي (لماذا هذه الخطة)

جوجل يعرف كل صفحاتنا (زحف الـ sitemap اكتمل) لكنه يرفض فهرستها بقرار جودة/أولوية، وليس بخطأ تقني:
- ✅ robots.txt سليم، sitemap كلها 200، meta robots سليم، canonical سليم
- ✅ لا عقوبات يدوية (تقرير Manual Actions: لم نرصد أي مشاكل)
- ❌ جوجل يصنّف 88 ألف صفحة قالبية متشابهة على نطاق شاب كمحتوى منخفض الأولوية

**الاستراتيجية:** لا نحارب القرار بكمّ الصفحات — نبني «نواة موثوقة» ثم نوسّع تدريجياً ببوابات قياس، بحيث كل دفعة جديدة تدخل على أساس ثقة مثبتة (حل دائم، لا مؤقت).

---

## المرحلة 0 — خط الأساس والأمان (يوم واحد، قبل أي تغيير)

| # | المهمة | التفاصيل | المخرَج |
|---|--------|----------|---------|
| 0.1 | توثيق خط الأساس | تصدير تقرير «فهرسة الصفحات» + «الأداء» من GSC (زر تصدير) وحفظ النسخة | ملف CSV مرجعي بالتاريخ |
| 0.2 | التحقق من وصول Googlebot فعلياً | في Cloudflare: Security → Events، فلترة User-Agent يحتوي Googlebot — التأكد من عدم وجود تحديات/حظر (Bot Fight Mode / Under Attack) | تقرير: 0 تحديات على Googlebot في آخر 30 يوم |
| 0.3 | فحص سرعة استجابة السيرفر لجوجل | تقرير Settings → Crawl stats في GSC: متوسط زمن الاستجابة يجب < 600ms | رقم مسجل؛ إن كان أعلى: مراجعة كاش الصفحات |
| 0.4 | نسخة احتياطية من جداول sitemap | `sitemap_urls_backup_20260917` موجودة — التحقق أنها سليمة قبل أي تعديل جديد | تأكيد وجود الباك أب |

**بوابة الخروج من المرحلة:** كل البنود فوق خضراء. لو وُجد حظر على Googlebot في 0.2 — إصلاحه أولوية قصوى قبل أي شيء آخر.

---

## المرحلة 1 — تركيز الترشيح وإيقاف الهدر (أسبوع 1)

### 1.1 تغيير sitemap-index: الترشيح للنواة فقط
**الملف:** `src/app/sitemap-index.xml/route.ts` + `src/lib/sitemap.ts`

الترتيب الجديد في الفهرس:
1. `static.xml` (الصفحات الثابتة: الرئيسية، التصنيفات، عن الموقع، DMCA)
2. `priority.xml` (موجود بالفعل: 3,000 فيلم + 2,000 مسلسل مفلترة — نواة قوية)

الشظايا (`movies-*.xml`, `series-*.xml`) **تبقى موجودة وتعمل وتُزاحف** — لكنها تخرج من قائمة الفهرس المُرسل مؤقتاً. هذا ليس حذف صفحات: جوجل سيستمر باكتشافها عبر الروابط الداخلية، ونعيد ترشيحها دفعة-دفعة في المرحلة 5.

**ملاحظة مهمة:** `SHARD_CACHE_CONTROL` = يوم كامل على الحافة — بعد النشر يجب تفريغ كاش الحافة (purge) لمسار `/sitemap*`.

### 1.2 صفحات الـ tags والفلاتر: `noindex`
- أي صفحة مولدة آلياً (فلترة genre/سنة/ترتيب) تحمل محتوى متداخل مع صفحات الأفلام نفسها → `robots: noindex, follow` عبر metadata في القالب.
- الصفحات الـ 53 المفهرسة حالياً **لا تُمس** — مراجعة يدوية لقائمة المفهرسات قبل تطبيق أي noindex.

### 1.3 تنظيف إعادة التوجيه والـ 404
- الـ 1 «خطأ إعادة توجيه»: فتح تقرير الفهرسة → drilldown → أخذ الرابط → فحص `curl -I` → إصلاح السلسلة (غالباً loop أو توجيه لميت).
- الـ 2 من 404: إما إعادة 301 للصفحة البديلة الصحيحة أو تركها (طبيعية).
- الـ 6 «صفحة تتضمن توجيه»: تُراجع فقط أن الوجهات صحيحة — لا إجراء إذا كانت مقصودة.

### 1.4 robots.txt: منع الزحف على مسافر بلا قيمة
الملف موجود في `public/` أو route — إضافة Disallow لأي مسارات فلاتر/باراميترات مولدة إن وُجدت (البحث والـ API محظورون بالفعل).

**بوابة المرحلة:** نشر + purge كاش + تأكيد في GSC أن `sitemap-index.xml` يعرض سايت مابين فقط. **لا تطلب «إعادة التحقق» في GSC هنا** — التقرير سيتحدث وحده خلال أسابيع.

---

## المرحلة 2 — محتوى فريد للنواة (أسبوع 1-3، بالتوازي)

هذه المرحلة هي **قلب الحل الدائم**. جوجل يرى 88 ألف صفحة بنفس الهيكل الجملة-بجملة؛ التمييز هو ما يقلب القرار.

### 2.1 اختيار Tier-1 (500 صفحة)
من `priority.xml` الحالي (5000): اختر 500 بمعيار:
- popularity عالية (موجودة في الاستعلام الحالي)
- سنة حديثة (2020+) — طلب بحث نشط
- slug نظيف (الفلتر الموجود في `rebuild-sitemap-quality.js`)

### 2.2 إثراء المحتوى (يبنى على خط الـ TMDB الموجود)
لكل صفحة من الـ 500 — الفرق المطلوب عن القالب الحالي:
1. **مقدمة تحريرية فريدة** (2-3 جمل بلغة بشرية عن سبب أهمية الفيلم/المسلسل — تُكتب أو تُولَّد ثم تُراجع يدوياً؛ نص TMDB الخام لا يكفي لأنه منسوخ على آلاف المواقع)
2. **قسم «معلومات سريعة»** ببيانات حقيقية: المدة، التصنيف العمري، البلد، اللغة، عدد الحلقات (للمسلسلات)
3. **قائمة الطاقم** بأسماء الشخصيات (ليس أسماء ممثلين فقط)
4. **عناوين ووصف فريد** لكل صفحة: قالب `شاهد {العنوان} ({السنة}) مترجم كامل — فور سيما` مع تغييرات حقيقية في الـ description
5. **Schema صحيح فقط** (VideoObject/MovieSchema ببيانات حقيقية من TMDB — بعد تنظيف الـ fake schema السابق في commit `a9e620de`)

### 2.3 منع تكرار القالب في المستقبل
- أي صفحة جديدة تدخل الفهرسة يجب أن تحقق حد أدنى: وصف ≥ 150 كلمة فريد + طاقم + بيانات ميتا مكتملة.
- إضافة فحص «نسبة التشابه» في سكربت الـ ingestion (`build-sitemap-urls.js`) — صفحة بلا إثراء لا تدخل نواة الترشيح أبداً.

**بوابة المرحلة:** 500 صفحة نواة مكتملة الإثراء، بمقارنة عشوائية 10 صفحات قبل/بعد.

---

## المرحلة 3 — الربط الداخلي (أسبوع 2-3)

### 3.1 وحدة «أفلام/مسلسلات مشابهة»
- البيانات متاحة (TMDB exports + جدول D1): استعلام similar حسب genre/سنة/شعبية.
- 6-8 روابط داخلية في كل صفحة عرض، بعنوان «قد يعجبك أيضاً» (موجود قسم — الضروري أن يكون روابط `<a>` حقيقية، لا JS lazy load بلا عناصر).

### 3.2 خريطة موقع HTML للزوار
- صفحة `/browse` (أو تعزيز صفحات التصنيف الموجودة): تصفح بالسنة/النوع/الأكثر شعبية — كل صفحة نواة تصل عبر ≤ 3 نقرات من الرئيسية.
- تربط من الفوتر، وتُفهرس (ليست noindex).

### 3.3 أجزاء ديناميكية على الرئيسية
- «أضيف حديثاً» و«الأكثر مشاهدة» على الرئيسية بروابط للصفحات الجديدة — الرئيسية أكثر صفحة يزحفها جوجل، كل رابط منها إشارة أولوية.

**بوابة المرحلة:** فحص عشوائي لأي صفحة نواة: ≤ 3 نقرات من الرئيسية + 6+ روابط داخلية واردة.

---

## المرحلة 4 — السلطة الخارجية (مستمرة من أسبوع 3)

بدون هذه المرحلة تبقى المراحل 1-3 محدودة الأثر — السلطة هي سقف الثقة:

| القناة | الإجراء | الأثر |
|--------|---------|-------|
| سوشيال + براند | صفحات رسمية نشطة (X, Facebook, Telegram) تشارك إضافات المحتوى | إشارات براند + زيارات مباشرة |
| مجتمعات عربية | مشاركة قيمة حقيقية في رِدِت/منتديات مشاهدة (لا سبام) | روابط + زيارات مستهدفة |
| YouTube | مقاطع قصيرة للأفلام الجديدة مع وصف يحمل الرابط | مصدر اكتشاف ثانٍ |
| SEO مباشر | إدراج في أدلة مواقع/مقالات مقارنة منصات (بجودة) | باك لينكس مؤسسة |

**قياس:** تقرير «الروابط» في GSC — الهدف نهاية الشهر الثاني: 20+ دومين مُحيل فريد.

---

## المرحلة 5 — التوسع التدريجي ببوابات (الحل الدائم)

القاعدة الدائمة: **لا يُرشَّح لجوجل أكثر من ما يفهرسه فعلاً.**

| الدفعة | المحتوى | الشرط للانتقال للدفعة التالية |
|--------|---------|------------------------------|
| Tier-1 | 500 صفحة نواة (مُرسلة في priority) | فهرسة ≥ 90% خلال 6 أسابيع + impressions تصاعدية |
| Tier-2 | `movies-0` + `series-0` (20 ألف) | تُضاف للفهرس **فقط بعد** تحقيق بوابة Tier-1 |
| Tier-3 | `movies-1..3` | نسبة فهرسة الدفعة السابقة ≥ 80% خلال شهر |
| Tier-4 | باقي الشظايا | نفس القاعدة |

**آلية القياس الأسبوعية (روتين ثابت):**
1. GSC → فهرسة الصفحات → قراءة: المفهرسة / تم اكتشافها / تم زحفها
2. GSC → الأداء → impressions آخر 7 أيام
3. تسجيل في جدول المتابعة (نفس ملف CSV الأسبوعي)
4. قرار: توسيع / انتظار / تدقيق

**شرط إيقاف وإعادة فحص:** أي انخفاض > 15% في عدد المفهرسات أسبوعاً بعد أسبوع → تجميد التوسع وتدقيق آخر تغيير.

---

## المخاطر والاحتياطات

| الخطر | الاحتياط |
|-------|----------|
| حذف شظايا من الفهرس يُفسَّر كتراجع | الشظايا تبقى حية ومتاحة؛ جوجل يعرفها من الـ history — لا إزالة نهائية |
| noindex ينزل صفحة مفهرسة بالخطأ | قائمة الـ 53 المفهرسة تُراجع يدوياً قبل أي noindex |
| كسر مسارات الروابط | لا تغيير في أي slug — الفلترة في طبقة sitemap فقط (نفس مبدأ `rebuild-sitemap-quality.js`) |
| كاش الحافة يخفي التغييرات | purge إلزامي لكل مسارات sitemap بعد كل نشر |
| طلبات فهرسة يدوية تُرفض وتؤذي | لا طلبات قبل اكتمال المرحلتين 2 و3 |
| تضخم Ads يقلل الجودة | الحفاظ على حد الإعلانات الحالي دون زيادة داخل صفحات النواة (أدلة CLS مضبوطة في commits الأخيرة) |

## الجدول الزمني المجمع

- **أسبوع 1:** المرحلة 0 كاملة + 1.1 + 1.3 + بدء كتابة محتوى Tier-1
- **أسبوع 2:** noindex للفلاتر + وحدة المشابه + بدء الربط الداخلي
- **أسبوع 3:** إكمال محتوى الـ 500 + خريطة HTML + أول روابط خارجية
- **أسبوع 4-6:** روتين القياس الأسبوعي، أول طلبات فهرسة يدوية للنواة المكتملة
- **أسبوع 6-8:** بوابة Tier-1 → إضافة Tier-2 إن تحقق الشرط
- **بعدها:** دورة توسع شهرية ببوابات، إلى أن تعود كل الشظايا للفهرس

## التعريف بالنجاح النهائي

- نهاية الشهر الثاني: 500+ صفحة مفهرسة (×10 الحالة الحالية)
- نهاية الشهر الرابع: 5,000+ مفهرسة و impressions بمنحنى صاعد
- الـ 88 ألف «اكتُشفت بدون فهرسة» تنكمش تدريجياً مع كل دفعة — وهو المؤشر الوحيد الذي يقيس إن كان جوجل غير رأيه فعلاً
