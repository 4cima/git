# 📋 ملخص تنفيذي — وضع مشروع 4cima (تاريخ التحديث: 2026-09-18)

> **استخدامه:** في أي محادثة جديدة اكتب: «اقرا HANDOFF-2026-09-18.md وكمّل من عندها» —
> أو انسخ محتواه لـDeepSeek كسياق جاهز.

> **نقطة التوقف:** آخر كوميت منشور ولايف = `a250bc6` (خطوة مسح كاش إلزامية بعد كل نشر) — ومعاه `3eb230b` (إصلاح similar: 145,068 → 14 صف/نداء).

---

## 0.5) تطويرات لاحقة نفس اليوم (18/9) — الجلسة الأخيرة

- **كاش الحافة شهر بدل سنة (`fbaea1f`):** كل المدد (تفاصيل/أنواع/ترقيم/لغات/قوائم/sitemap/404/APIs) بقت `MONTH = 30 يوم` — الاشتراك شهري. التحقق لايف: `s-maxage=2592000` على النسخ الجديدة. ملاحظة: النسخ المخزنة قبل النشر لسه بهيدرات قديمة (أسبوع) لحد أول `purge_everything` مع المزامنة الجاية.
- **CSP Report-Only (`fbaea1f`):** هيدر `Content-Security-Policy-Report-Only` في `next.config.ts` على `/:path*` — مصادر Adsterra (professionalsusceptible.com, al5sm.com, highrevenueformat.com) + TMDB وصور. مش بيمنع حاجة (مراقبة بس في الكونسول). DeepLX وCloudflare **ماتضافوش** (استدعاءات سيرفر-للسيرفر بس، مش من المتصفح). الـCSP المفروض القديم زي ما هو.
- **توحيد الفروع:** `.github/workflows/deploy.yml` بقى يشتغل على `cloudflare-migration` + `main`، وmain اتزامن مع الفرع. فرع `cloudflare-migration` اترك أرشيف (متمسحش).
- **تنظيف كود يتيم:** اتحذف 7 ملفات بصفر مستخدمين: FilterTabs (فروع gaming/software/anime بقت ميتة معاه) • UnifiedPlaceholder • ContentGrid • common/Breadcrumbs (لابل anime) • types/unified-section.ts (تعريف ContentType كامل) • lib/tmdb.ts • route `/api/anime` (مفيش ولا منادي). MovieCard اتشال منه `isAnime` (مفيش صفوف anime في D1 أصلاً — مفيش عمود content_type في movies/tv_series).

---

## 0.6) جلسة 18/9 (مساءً) — الـ45 سلاج + توثيق الكاش + اكتشاف similar

- **الـ45 سلاج اتنفذوا بالكامل ولايف:** backup إلزامي `slug_fix_backup_20260918` (47 صف: 1 فيلم + 44 مسلسل + 2 صف بحث) • `king-kong-1784684809695` → `king-kong` والـ44 مسلسل صيني (سلاج بادئ بشرطة) → الـtmdb_id كرقم • اتحدث كمان `short_titles_lookup` (صفان) و`list_movies_genre` (صف king-kong) — القوائم التانية وصفر فيها منهم • **45 redirect دائم** في `next.config.ts` (الصيغة العملية 308 — صيغة Next للـpermanent، نفس قيمة SEO) • sitemap_urls اتبنى تاني • **purge محدد** لـ111 رابط (القديمة + الجديدة + ملفات الـsitemap — من غير purge_everything). التحقق لايف: القديمة 308 للجديدة، الجديدة 200، الرئيسية سليمة.
- **ملاحظتان مهمتان:** (1) الـ44 مسلسل **ماكانوش في الـsitemap قبل ولا بعد** — فلتر الجودة `SLUG_QUALITY_WHERE` في build-sitemap-urls.js بيستبعد أي سلاج أرقام بس أو بيبدأ بشرطة، فمفيش حاجة اتغيرت لهم هناك. (2) **`meta.changes` في D1 بيرجع أرقام مشله للـUPDATE** (رجّعت 11 لتحديث صف واحد) — التحقق لازم يكون بـSELECT بعد الكتابة مش بعدّاد D1.
- **enableCacheInterception:** القرار (رفض) اتوثق جوه `open-next.config.ts` بتعليق (`05a8566`) — القيمة أصلاً مش موجودة = false.
- **⚠️ اكتشاف similar — اتنفذ بموافقة إسلام (`3eb230b`):** استعلام «قد يعجبك أيضاً» في `src/lib/similar-server.ts` كان بيكمل يقرأ **145,068 صف/نداء فعليًا** (اتقاس على الإنتاج) رغم أن الكود مكتوب إنه ~15 — السبب: شرط الـOR `(filter_status IS NULL OR filter_status IN (...))` بيخلي SQLite يختار **MULTI-INDEX OR** بيمسح `idx_movies_filter` مرتين (كل الكتالوج النظيف) ويهمل فهرس tmdb_id الموجود أصلاً (القاعدة من غير sqlite_stat1 فتقديراتها بايظة). **العلاج المنفذ: إعادة كتابة الشرطين بـ`IFNULL(filter_status,'clean') IN ('clean','reviewed_approved')` (نفس المنطق حرفيًا) → القياس الفعلي نزل لـ14 صف للأفلام و24 للمسلسلات** — من غير أي فهرس جديد. التحقق بعد النشر: tsc صفر • eslint صفر errors (8 warnings `any` قديمة) • EXPLAIN = `SEARCH ... USING INDEX sqlite_autoindex_*_1 (tmdb_id=?)`. نفس نمط الـOR موجود في `src/app/page.tsx` (محمي) و`home-sections-query.ts` (استعلامات نادرة بعد كاش الرئيسية) — مش هيتلمسوا. ملاحظة تشغيلية: توكني المحلي مالوش صلاحية GraphQL Analytics (`d1AnalyticsAdaptiveGroups`) — متابعة منحنى rows_read بالساعة من داشبورد D1 عند اسلام بس.

---

## 0.8) جلسة 18/9 ليلًا — تنظيف شامل منشور + إضافات المزامنة

- **تنظيف الكود اليتيم (المرحلة الأولى `fa2a77c` + الشاملة `afc9e16`):** الإجمالي **99 ملف، -10,885 سطر** — نظام game/software/quran + 88 ملف يتيم مؤكد بصفر مراجع في الريبو كله (استيراد/require/dynamic/side-effect/كومنت): كامل نظام LUMEN UI (11 مكون + barrel — **ui/Skeleton فضلت: HomePageClient بيستخدمها**) + ToastContext/ToastProvider + الهيرو القديم (QuantumHero/MegaHero/MarqueeBanner — الهيرو الحقيقي تنفيذ HomePageClient نفسه) + مشغلات وسيرفرات قديمة (7 ملفات) + 15 hook يتيم + أنواع database/media/appPromo + styles قديمة + lib/groq.ts. الحذف كان على دفعات 5 مع tsc بعد كل دفعة وتراجع تلقائي.
- **اتأكدنا منهم فما اتحذفوش:** deadDeliveryGuard (متستورد بـside-effect من adsterraQueue — نظام الإعلانات الحي) • ui/Skeleton • الـ3 ملفات .d.ts (ambient) • ملفات الـGROQ السبعة (كلها مستوردة بأدوات شغالة — INGEST-* مدعومة بسكربتات fetch/ingest، translation-service في سلسلة المزامنة، verify-build-secrets في deploy.yml) — **وقايمة الأسرار فيه فضلت زي ما هي عمدًا: بتراقب مفتاح Groq الميت اللي لسه قاعد في .env.local (ملف محمي) عشان مايسربش للبناء**.
- **قوائم الأنواع دخلت سلسلة المزامنة (`b54298c`):** `rebuildGenreLists()` خطوة 4 — short-titles → purge → genre-index → **genre-lists** → sitemap. عشان `list_*_genre` ما تتجمدش تاني (صفحات /page/N والـAPIs). ملاحظة: السكربت بياخد ~35-40 دقيقة، وفلتره `filter_status='clean'` أضيق من شرط الزائر (محدث بالتعليق).
- **تحسين صفحات التصنيفات (`2b39ae0`):** أول صفحة بقت partition+probe — 409,578→75 صف أفلام و101,134→362 مسلسلات، مطابقة حرفية للاستعلام الحي (متحقق 5 أنواع). `list_*_genre` نفسها **مش** مصدرها (ترتيب «التصنيف الأساسي أولًا» بيغير المحتوى).
- **ترجمة المسلسلات الصينية (`a67f320`):** 47/47 اترجموا بـllama-3.3-70b عبر OpenRouter — **GROQ_API_KEY في .env.local ملغي (401) — إسلام يحتاج يستبدله أو يحذفه** (السطر مكانها لأن الملف محمي، والحارس في verify-build-secrets لسه بيراقبه).
- **⚠️ تكامل Workers Builds في داشبورد كلاود فلير بيفشل مع كل push** (مسار موازٍ لـGitHub Actions من غير خطوة مسح الكاش) — التوصية: فصله من Settings → Build. النشر الرسمي = GitHub Actions وهو سليم 100%.
- **⚠️ فخ similar تاني — صفحات التصنيفات أول صفحة (اتنفذ بموافقة إسلام `2b39ae0`):** نفس فخ الـOR — `/genres/[slug]` + `/movies/genres/[slug]` + `/series/genres/[slug]` (صفحة أولى بس) كانوا بيسألوا الجدول الحي بـ`EXISTS(json_each) + OR` → **409,578 صف/render أفلام + 101,134 مسلسلات (614ms+415ms)**. **العلاج المنفذ:** مصدر الصف بقى partitions الأنواع `movies_by_genre` / `series_by_genre` (الـPK `genre_id, popularity, id` بيخلي الاستعلام يمشي بترتيب الفهرس ويقف عند LIMIT) + PK probe على الحي بفلتر الحالة/السنة + قواعد تفريق TV الخمسة (28/12/27/14/878) لسه بتتطبق بـbuildTvGenreClause على الصفوف المنضمة (مخزون 1000). **المطابقة اتأكدت: أعلى 25 مطابقة حرفيًا للاستعلام الحي في 5 أنواع (أكشن/رعب/إثارة/خيال علمي/كوميديا — أفلام ومسلسلات).** المقاس بعد: أفلام **75 صف** (كان 409,578) • مسلسلات خاص **362** (كان 101,134) • إثارة متعدد القوائم 19K (أسوأ حالة، كان 101K).
- **⚠️ ملاحظة تشغيلية مهمة (18/9):** جداول `list_*_genre` **مش بتتبني في سلسلة المزامنة** — كانت متجمدة من آخر تشغيل يدوي (صفحات /page/N والـAPIs كانت بتخدم محتوى قديم) — اتبنت يدوي بـ`node scripts/6-precompute-genre-lists.js`. ترتيبها الداخلي «التصنيف الأساسي أولًا» بيخلي محتواها يختلف عن الحي في الأنواع الكبيرة — مبنية على فلسفة مقصودة. **يُرى إضافتها لسلسلة المزامنة (قرار إسلام).** أما `*_by_genre` فبيتجددوا تلقائيًا بعد كل مزامنة (وهما مصدر الصفحات المعدلة).
- **الأماكن اللي اتقاست وسليمة (مفيش تعديل):** صفحات التفاصيل والـAPIs بـslug (صف واحد بالفهرس) • الـOR الرقمي في `/api/movies/[slug]` (كل طرف مفهرس) • البحث FTS + short_titles_lookup • anti-join الرئيسية (page.tsx محمي وأصلاً كفء: 125/274 صف) • home-sections anti-join (~610 صف) • قوائم العربي (2.4K صف + TEMP B-TREE صغير — مقبول).
- **ترجمة المسلسلات الصينية (اتنفذت بموافقة إسلام `a67f320`):** 47 صف tv_series سلاج رقمي (26 ترجمة قمامة + 21 اسم CJK خام) اترجموا بـ**llama-3.3-70b عبر OpenRouter** — **GROQ_API_KEY بيرجع 401 (ملغي) رغم شكله السليم — محتاج تدوير/استبدال من إسلام**. المسار: EN من TMDB لما يتوفر (5) وإلا الاسم الأصلي ZH/JA. Backups: `chinese_titles_backup_20260918` + `chinese_titles_stl_backup_20260918` • short_titles_lookup صفّي 298527/302293 اتحدثوا • تحقق SELECT 47/47 • purge محدد 47 صفحة والتحقق لايف 200 + الأسماء الجديدة ظاهرة. **ملاحظة متبقية:** `name_en` لسه فيه الاسم الصيني الأصلي (بيظهر كسطر فرعي تحت العربي في بعض الواجهات) — لو إسلام عايز يتظبط بقي قرار منفصل.

---


- **الإصابة اللي اسلام شافها:** صفحات مكسورة من غير ستايل/سكريبت — الكونسول فيه `Refused to apply style` و`404` على ملفات `_next/static/chunks/main-app-*` و`css/*` (بتترجع text/html).
- **السبب الجذري:** كل نشر بيبدّل ملفات `_next/static` بأسماء (hashes) جديدة — وصفحات HTML المكتاشة على الحافة (**شهر** — الرئيسية/التفاصيل/القوائم) بتطلب الأسماء القديمة → 404. `deploy.yml` **كان مالوش أي خطوة مسح كاش** — يعني كل نشر كان حادثة مضمونة من أول نسخة مكتاشة لحد ما الكاش ينتهي أو مزامنة تمسح.
- **العلاج الفوري:** `purge_everything` يدوي (نفس توكن الـpurge) — الموقع اتعافى فورًا (اتأكدت: الرئيسية MISS بنسخة جديدة + chunks بتاعتها 200 + صفحة تفاصيل 200).
- **العلاج الدائم (`a250bc6`):** خطوة **`Purge edge cache`** في `.github/workflows/deploy.yml` بتشتغل بعد `wrangler deploy` مباشرة (purge_everything) + **السكرتين `CF_CACHE_PURGE_TOKEN` و`CF_ZONE_ID` اتضافوا لـGitHub Actions secrets** (من قيم `.env.local` نفسها). اتأكدت على أول نشر حقيقي: الخطوة ✓ والموقع سليم بعده.
- **قاعدة جديدة معمارية:** النشر من غير مسح الكاش = حادثة مضمونة — ممنوع أي تعديل على الـworkflow يشيل الخطوة دي. ملاحظة جانبية: أول طلبات بعد أي نشر بتكون MISS (أبطأ شوية) — ده طبيعي ومطلوب.


---

## 0) آخر التطورات (18/9) — كاش الحافة جوه الـWorker لايف

**الاكتشاف الجذري:** D1 كان بيحرق 417–757M صف/يوم على **90K طلب Worker فقط** (~3,300 صف لكل طلب!). السبب: كتابة الـISR في KV بيفشل بصمت على الخطة المجانية (حد **1,000 كتابة/يوم**) — الدليل: صفحات سخونة زي `/movies/genres/action` ملهاش أي مدخل في كاش أي بيلد من ~300 بيلد، ومقبرة 8,490 مفتاح قديم في الـKV. يعني الـISR كان اسمه على الحيط: كل زحف = إعادة توليد كاملة من D1.

**الحل المنفذ والمنشور** (`2fd4b1f` → `30c0f90`):
- `src/edge-cache-worker.ts` = الـentry الجديد في wrangler.jsonc: طبقة `caches.default` (بلا حدود مجانية) قبل الـOpenNext handler — **المدد القصوى (`30c0f90`)**: التفاصيل/الأنواع/الترقيم/اللغات + الـAPIs الستة **سنة** • الرئيسية/القوائم + sitemap + 404s **أسبوع** — لأن المحتوى ثابت والـinvalidation بيجي من `purge_everything` بعد المزامنة (وزر الأدمن) مش من انتهاء المدة. كل النسخ فيها `stale-while-revalidate` ساعة + `stale-if-error` يوم، والمتصفح 5 دقايق (الـ404 صفر). المفتاح = path من غير query.
- `revalidate` صفحات التفاصيل 60 → **3600**.
- التحقق لايف تم: `x-edge-cache: HIT` على كل الأنواع • الأدمن 307 من غير كاش • البحث والـAPIs عدّوا زي الأول • الـpurge بعد المزامنة بيضبط التحديثات (اتجرب).
- **حل لغز الـ4 ساعات (مهم جدًا):** مصدره كان إعداد **Browser Cache TTL = "4 hours"** في Caching → Configuration (مش القواعد نفسها). اتصلّح لـ**Respect Existing Headers** + اتنظفت القواعد المتداخلة (اتحذفت: Cache Listing Pages + Cache Sitemap — الباقي: Cache TMDB Images + Cache pages من غير Browser TTL). النتيجة (`3b7bb3d`): هيدرات نسخ الكود بتوصل المتصفح كما هي — الصفحات `max-age=300`، الـAPIs `max-age=300` مع `s-maxage` بتاعتها (1800/300). **قاعدة:** أي قاعدة كاش جديدة في الداشبورد ممنوع فيها Browser TTL override.
- **تنظيف مقبرة الـKV (بدأ):** اتأكد إن الـ8,780 مفتاح كله قمامة (أحدث كتابة في الـnamespace كله 2026-09-05 — البيلدات الجديدة بتكتب صفر). اتحذفت أول **1,000** بنجاح (متبقي ~7,780) • السكربت `scripts/cleanup-kv-graveyard.js` (بيدعم `--dry` + حماية تلقائية لأي بيلد فيه كتابة أحدث من 7 أيام + سقف 1,000/تشغيلة) + سير العمل `.github/workflows/kv-cleanup.yml` **منشورين**. ⚠️ الـcron التلقائي بيشتغل بس من الفرع الافتراضي (main) — لحد الدمج: شغّل يدويًا `gh workflow run kv-cleanup.yml --ref cloudflare-migration` أو `node scripts/cleanup-kv-graveyard.js` محليًا (~8 تشغيلات متبقية).
- **⚠️ حادثة حرجة اتحلت (18/9):** Cache API بتكلاود فلير **بتتجاهل Vary تمامًا** (مطابقة بالـURL بس) — تنقلات الموقع والـprefetch بيبعتوا طلبات RSC، فاتخزن رد RSC مكان صفحة الـHTML في الكاش بسنة → زوار بيشوفوا **صفحات فاضية بـJSON خام**. الإصلاح لايف (`4beba90`): طلبات الراوتر (هيدرات rsc/next-router-*) بتتعدي من غير تخزين خالص + المخزّن لازم `text/html` (والـAPIs `application/json`) + شيل Vary من النسخ المخزنة. اتأكدت بالتسلسل الكاسر سابقًا (HTML → RSC → HTML = بيفضل HTML) والموقع رجع كامل بصريًا.
- **بعد أي `build:cloudflare` محلي: امسح `.open-next` قبل `npx tsc`** (allowJs بيتبع الباندل المولّد ويرمي أخطاء وهمية).
- متابعة الأثر: قراءة D1 اليومية عبر GraphQL (`d1AnalyticsAdaptiveGroups` بتوكن البيئة) — القاعدة قبل النشر: 599M (15/9) • 757M (16/9) • 417M (17/9). أول يوم كامل بعد النشر = 2026-09-18.

---

## 1) المشروع والبيئة

- **الموقع:** 4cima.com — منصة أفلام ومسلسلات (Next.js App Router على Cloudflare Workers + D1)
- **الفرع:** `cloudflare-migration` | **النشر:** أي `git push` → GitHub Actions يبني وينشر تلقائيًا
- **القاعدة الإنتاجية:** D1 `b50ec43e-...` (1.1GB، 49 جدول)
- **سيرفر التجربة:** منفذ 3000
- **قواعد ثابتة:** ممنوع commit/push/deploy بدون أمر صريح من اسلام • ممنوع لمس: `src/lib/db.ts` / `src/app/page.tsx` الرئيسية / Navbar / player-worker / ads.txt / package.json / .env • التقارير بصيغة STOP (A,B,C...) • الردود بالعربي المصري وبساطة (اسلام صفر برمجة)
- **الهدف العام:** فاتورة Cloudflare $0 مع زيادة الزوار 10-15× — D1 rows read عند 19.7B من 25B المجانية

## 2) آخر الحالة المنشورة (كلها لايف ومتحقق منها)

| Commit | الموضوع |
|---|---|
| `ce5ece5` | تنظيف sitemap: حذف 338 سلاج قمامة من D1 + فهرس التصنيفات المجمّع + سلاج-جنيريتور موحد (backup: `sitemap_urls_backup_20260917`) |
| `5bdcf14` | علاج صفحات الأيتام (87,170 "مكتشفة غير مفهرسة"): قسم «قد يعجبك أيضاً» SSR + شارات التصنيفات روابط + ترقيم ساكن `/movies/genres/{slug}/page/N` + anti-join في الرئيسية |
| `c292c32` | سرعة: صورة الخلفية 668KB→63KB + الإعلان الفاشل يفضل محجوز مخفي (CLS) + لافتة الهيرو من غير 120 re-render/ثانية (INP) |
| `d443c55` | فهرس التصنيفات المادي: `movies_by_genre` (206K) + `series_by_genre` (65K) + جداول الممنوعات — فلتر `?genre=` بـ~50 صف بدل مسح الكتالوج + حذف 7 ملفات ميتة + Actions v5 |
| `1e252ad` + `20cb251` | أتمتة ما بعد المزامنة |

## 3) سلسلة ما بعد المزامنة (تلقائية الآن داخل `3-sync-to-d1.js`)

بعد نجاح أي مزامنة محتوى، بالترتيب: قوائم البحث القصيرة → مسح كاش الحافة → **إعادة بناء فهرس التصنيفات** → **إعادة بناء sitemap_urls**.
- الفشل في أي خطوة = رسالة تحذير فقط، المزامنة نجحت أصلًا.
- أوضاع المزامنة: عادية / `--limit=` / `--tmdb-id=` / `--ids-file=` تمر بالسلسلة • `--rebuild-short-titles` مستقل • `--all` معطّل.

## 4) قواعد معمارية مهمة (لا تُخترق)

- **ممنوع `NOT IN (SELECT...)` و `json_each` على الـhot path** — بيمسحوا الكتالوج كله (استبدلهم بـanti-join أو الجداول المجمّعة).
- `loading.tsx` الجذري **محذوف عمدًا** (حل soft-404) — **ممنوع إرجاعه نهائيًا**.
- جداول الفهرس = لقطة: أي تعديل على `buildTvGenreClause` في `genre-siblings.ts` يستلزم `node scripts/build-genre-index.js`.
- **كاش الحافة الجديد (`edge-cache-worker.ts`):** allowlist بس — ممنوع تكاش /admin • /api/ads • /api/continue-watching • /search • /profile والـAPIs غير المعلنة • التحديثات بعد المزامنة بيمسحها `purge_everything` (الموجود في السلسلة) • الهيدر `x-edge-cache: HIT|MISS` للتأكد لايف • **ملاحظة تشغيلية:** أول طلب بعد المسح MISS والنسخة بتتحط لحظة بعده — استنى ثواني قبل ما تحكم على أي فحص.
- كاش HTML على الحافة بقواعد الداشبورد **مستحيل** (الصفحات تتولد جوه Worker) — اللي بيخدم هو `edge-cache-worker.ts`. **تنظيف 2026-09-18:** بقت قاعدتين بس في الداشبورد (TMDB Images + Cache pages، الاتنين من غير Browser TTL) + الإعداد العام Respect Existing Headers — ممنوع إرجاع أي Browser TTL override.
- التوكنات في `.env.local`: الـpurge بـ`CF_CACHE_PURGE_TOKEN` (+`CF_ZONE_ID`) — توكن البيئة العام مالوش صلاحية purge • D1 بـ`CLOUDFLARE_D1_TOKEN`.
- قواعد كاش كلاود فلير شغالة: صور TMDB (HIT مؤكد). قواعد صفحات HTML موجودات بس ماتشتغلش (السبب فوق) — مفيش ضرر منها.

## 5) المفتوح / المؤجل (بترتيب القيمة)

1. ~~إصلاح similar~~ **اتنفذ ولايف `3eb230b`:** إعادة كتابة شرطين في `src/lib/similar-server.ts` (سطر 49 و57): `(filter_status IS NULL OR filter_status IN ('clean','reviewed_approved'))` → `IFNULL(filter_status,'clean') IN ('clean','reviewed_approved')` (نفس المعنى حرفيًا، من غير OR) + تعليق يوثق السبب. **الأثر المقاس: 145,068 → 14 صف/نداء للأفلام و38,340 → 24 للمسلسلات** (التفاصيل في البند 0.6). متابعة الأثر الكلي على rows_read من داشبورد D1.
2. **مراقبة النتائج** (أسبوعين-٤ أسابيع): **الأول الأن: D1 rows read بعد كاش الحافة** (البند 0 فوق) — كان المتوقع يهبط حادًا من 417-757M/يوم • جوجل كونسول — "تم الزحف" تعلى ثم "مفهرسة" تنزل من 87,170 المعلقة • تقرير السرعة (CWV كان فاشل: LCP 3.6s موبايل / CLS 0.7 / INP 773ms) يتحسن تدريجيًا.
3. **الإعلانات:** Adsterra بيرجع أخطاء 500 (من سيرفرهم) — متابعة مع الشركة. أول ما تستقر → مهمة CSP/COOP مع اختبار كامل.
4. ~~تفعيل كاش الـAPIs على الحافة~~ **اتنفذ** (`3b7bb3d` بعد تنظيف قواعد الداشبورد + إصلاح Browser Cache TTL) — بقيادة دراسة `enableCacheInterception`: **التوصية = رفض** — الـedge-cache-worker بيكاش على مستوى الـWorker كله (صفحات + APIs) بـ`caches.default` بدون حدود، بينما الـcache interception بتاع OpenNext بيشتغل بس جوه الـDO ShardedTagCache path وبيلف على الـtag cache مع كل طلب — يعني تكرار للطلبات ويستهلك من حد الـDO (100k/يوم مجانًا) بدون فايدة إضافية عندنا. **القرار اتوثق في `open-next.config.ts` (`05a8566`).**
5. ~~45 سلاج بطوابع زمنية~~ **اتنفذت بالكامل 18/9** (backup `slug_fix_backup_20260918` + UPDATE + 45 redirect في `next.config.ts` + rebuild sitemap + purge محدد 111 رابط) — الباقي منها: الـ44 مسلسل الصيني عندهم title_ar قمامة (ترجمة آلية بايظة) — مشكلة بيانات منفصلة.
6. **صيانة الـKV:** مقبرة 8,490 مفتاح من ~300 بيلد قديم (الحذف بحد أقصى 1,000/يوم مجانًا) • قرار R2 بدل KV لو هنرجع نعتمد على الـISR route (توكن الحساب مالوش صلاحية R2 — محتاج توكن/تفعيل).
7. مؤجل: تحسين CSS الـcritical • keyset pagination • WebP (محتاج ميزة مدفوعة) • تدوير التوكنات (قرار إداري عند اسلام).

## 6) فهرس الأوامر السريعة

```bash
node scripts/3-sync-to-d1.js            # مزامنة محتوى + السلسلة التلقائية كاملة
node scripts/build-genre-index.js       # إعادة بناء فهرس التصنيفات (يدوي لو لزم)
node scripts/build-sitemap-urls.js --remote --apply   # إعادة بناء الخريطة (يدوي لو لزم)
npx tsc --noEmit                        # فحص الأنواع (بعد build محلي: امسح .open-next الأول)
curl -s -D - -o /dev/null "https://4cima.com/movies/SLUG" | grep -i x-edge-cache   # التأكد إن الكاش HIT
```
