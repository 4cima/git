# 📋 ملخص تنفيذي — وضع مشروع 4cima (تاريخ التحديث: 2026-09-18 ليلًا)

> **استخدامه:** في أي محادثة جديدة اكتب: «اقرا HANDOFF-2026-09-18.md وكمّل من عندها» —
> أو انسخ محتواه لـDeepSeek كسياق جاهز.

> **نقطة التوقف:** آخر كوميت منشور ولايف = `afa3528` — وآخر كوميت كود = `3b6e74b`
> (إصلاح استعلامات العربي بصيغة IFNULL). النشر دايمًا عبر GitHub Actions (مع مسح كاش إلزامي).

---

## 0.10) جلسة 19/9 — صفحات اللغة على فهرس مركّب + anti-join (د0bea93 منشور ولايف)

**الرصد (D1 Metrics آخر 12 ساعة):** استعلامات اللغة كانت 25.6% من وقت D1 (Movies 14.97% + TV 10.61%).

- **التشخيص:** مش فخ الـOR — IFNULL متطبق فعلًا في صفحات اللغة منذ 3b6e74b. السبب: **مفيش فهرس (لغة، شعبية)** فالمخطط كان بيمسح كل صفوف اللغة على idx_*_original_lang ثم يفرزها مؤقتًا (TEMP B-TREE) ليأخذ 25 — الإنجليزية 69,090 صف × مضاعفة json_each = **217,493 صف/نداء** (والـTV 43,353).
- **الإصلاح (d0bea93):** فهرسان مركّبان جزئيان على الإنتاج: `idx_movies_lang_listing` / `idx_tv_lang_listing` (original_language, popularity DESC, id DESC — بنفس بوابات idx_*_listing: IFNULL + سنة ≥ 2000) + سكربت الصيانة `scripts/maintenance/create-lang-listing-indexes.js` (idempotent). الصفحتان بدلوا json_each بـanti-join على `excluded_genre_*_ids` (مطابق حرفيًا لـ/api/movies و/api/series فأول صفحة و«تحميل المزيد» متطابقين) + tiebreak بالـid. اللغة المتعددة (zh,cn) بقت **UNION ALL** لكل لغة (الـIN بيسقط ترتيب الفهرس: 16,670→136).
- **النتيجة المقاسة على D1 الإنتاج:** 20/20 استعلامًا نفس النتائج بنفس الترتيب — الإجمالي 31,515→1,220 صف (26×)، الإنجليزية أفلام 217,493→50 والمسلسلات 43,353→58.
- **⚠️ قاعدة جديدة:** أي تعديل على بوابات (فلتر الحالة/السنة) في استعلامات اللغة يستلزم فهرسًا مطابقًا — البوابات مكتوبة 3 مرات (الاستعلامان + الفهرسان) ولازم تتزامن.
- **⚠️ مصدر الاستعلام الأول في المتريكس (`SELECT * FROM movies ORDER BY rowid LIMIT ? OFFSET ?` — 21.4% من D1) = `scripts/backup-d1.js:150`** — النسخ الاحتياطي اليومي (backup-d1.yml، 2 صباحًا UTC). **اتصلح بعدها في نفس اليوم (`4f7f719`) — keyset pagination** بـ`WHERE id > ? ORDER BY id` (id = rowid alias — تحقق صفر فروق): أعمق نداء 145,000 صف ← 500، والقراءة اليومية ~28.4M ← ~210K. **التحقق الصارم: تشغيل القديم والجديد ومقارنة بايتية كاملة** (145,066/38,338/9,500/9,947 صف — تطابق بايتي خارج طابعين زمنيين). ⚠️ `list_*_genre` فضلت على OFFSET عمدًا (PK مركّب + صغيران). النسخ الاحتياطية قبل التعديل في `D:\tmp\backup-before-fix\`.

---

## 0.9) جلسة 18/9 ليلًا (الأخيرة) — تنظيف شامل + 3 إصلاحات أداء + ترجمة

السلسلة الكاملة بالترتيب (كلها منشورة ولايف ومتحقق منها):

| Commit | إيه |
|---|---|
| `fa2a77c` | تنظيف يتيم 1: نظام game/software/quran + 11 ملف يتيم (15 ملف، -2,400 سطر) |
| `a67f320` | ترجمة 47 مسلسل صيني بـllama-3.3-70b (OpenRouter) — backup + purge محدد |
| `b54298c` | `rebuildGenreLists()` دخل سلسلة المزامنة (خطوة 4) |
| `2b39ae0` | صفحات التصنيفات أول صفحة: partition+probe (409,578→75 صف) |
| `afc9e16` | تنظيف يتيم 2 الشامل: **88 ملف، -8,485 سطر** (صفر مراجع في الريبو كله) |
| `0142b90` | حذف الأرشيف: `_archive-pre-cf/` + `audit-report/` (27 ملف، -5,069 سطر) |
| `3b6e74b` | استعلامات العربي بصيغة IFNULL (رصد 80K صف/نداء = 19% من D1) |

**تفاصيل سريعة:**
- **التنظيف الشامل (الإجمالي 130 ملف و~16K سطر في اليومين):** مسح بـbasename عبر الريبو كله (استيراد/require/dynamic/side-effect/كومنت) + tsc بعد كل 5 ملفات مع تراجع تلقائي. **اتأكدنا منهم فما اتحذفوش:** `deadDeliveryGuard` (side-effect import من adsterraQueue — الإعلانات الحية) • `ui/Skeleton` (HomePageClient بيستخدمها) • الـ3 ملفات `.d.ts` • ملفات الـGROQ السبعة (كلها مستوردة بأدوات شغالة).
- **صفحات التصنيفات (`2b39ae0`):** أول صفحة (genres/[slug] + movies/genres + series/genres) كانت بقرأ **409,578 صف أفلام + 101,134 مسلسلات/نداء بارد** (json_each + OR → MULTI-INDEX OR). المصدر بقى **partitions `movies_by_genre`/`series_by_genre`** (الـPK بيخلي الاستعلام يقف عند LIMIT) + PK probe بفلتر الحالة/السنة + قواعد تفريق TV الخمسة (28/12/27/14/878 — معلنة في `TV_DISAMBIGUATED_GENRES` في genre-siblings.ts). **المطابقة اتأكدت حرفيًا (top-25 في 5 أنواع).** بعد: 75/362 صف (وأسوأ حالة إثارة 53 متعدد القوائم 19K).
- **قوائم الأنواع (`b54298c`):** `list_*_genre` كانت **مش بتتبني في سلسلة المزامنة** (متجمدة — صفحات /page/N والـAPIs كانوا بيخدموا محتوى قديم). اتبنت يدوي + السكربت دخل السلسلة (بياخد ~35-40 دقيقة). ⚠️ ترتيبها «التصنيف الأساسي أولًا + سقف 500» بيغير المحتوى عن الحي — فمصلحهاش مصدر لصفحات أولى.
- **استعلامات العربي (`3b6e74b`):** رصد 80K صف/نداء (19% من D1) على الداشبورد — نفس علاج similar بـIFNULL على 6 استعلامات (api/listing/arabic + genres/arabic + home-sections ARABIC). النتائج متطابقة 100% والخطة مثبتة على idx_*_original_lang (2,423/1,565 صف).
- **الترجمة الصينية (`a67f320`):** 47 صف tv_series سلاج رقمي (26 ترجمة قمامة + 21 اسم CJK خام) — **GROQ_API_KEY بيرجع 401 (ملغي)** فالتنفيذ عدّى على **OpenRouter بنفس الموديل llama-3.3-70b**. المسار: EN من TMDB لما يتوفر (5) وإلا الاسم الأصلي ZH/JA. Backups: `chinese_titles_backup_20260918` + `chinese_titles_stl_backup_20260918` • تحقق SELECT 47/47 • purge محدد 47 صفحة. **متبقي اختياري:** `name_en` لسه CJK (بيظهر كسطر فرعي) — قرار منفصل.
- **⚠️ مفتاح GROQ_API_KEY في `.env.local` ملغي (401)** — محتاج إسلام يستبدله أو يحذفه. السطر مكانه (الملف محمي) و**قايمة الأسرار في `verify-build-secrets.js` متسابت عمدًا** عشان تستمر في مراقبة المفتاح الميت مايسربش للبناء.

---

## 0.7) حادثة 18/9 مساءً — النشر من غير مسح كاش كان بيكسر الموقع (اتصلحت دائمًا)

- **الإصابة:** صفحات مكسورة (404 على `_next/static/chunks/*` و`css/*` بتترجع text/html).
- **السبب الجذري:** كل نشر بيبدّل الـhashes وصفحات HTML المكتاشة (شهر) بتطلب الأسماء القديمة → 404. `deploy.yml` كان مالوش خطوة مسح كاش.
- **العلاج الدائم (`a250bc6`):** خطوة `Purge edge cache` (purge_everything) في deploy.yml بعد كل نشر + سكرتين `CF_CACHE_PURGE_TOKEN`/`CF_ZONE_ID` في GitHub Actions secrets.
- **قاعدة معمارية:** النشر من غير مسح الكاش = حادثة مضمونة — ممنوع أي تعديل يشيل الخطوة دي. أول طلبات بعد النشر MISS — طبيعي.
- **⚠️ تكامل Workers Builds في داشبورد كلاود فلير:** مسار نشر موازٍ لـGitHub Actions **من غير خطوة مسح الكاش** وبيفشل مع كل push (آخر فشل build #89499810 خطوة Deploying). التوصية: فصله من Settings → Build — النشر الرسمي = GitHub Actions (سليم 100%).

---

## 0) كاش الحافة جوه الـWorker (لايف من 18/9)

- **السبب الجذري لتاكل D1:** كتابة ISR في KV بتفشل بصمت على الخطة المجانية (1,000 كتابة/يوم) = كل زحف كان إعادة توليد كاملة (3,300 صف/طلب!).
- **الحل:** `src/edge-cache-worker.ts` — طبقة `caches.default` قبل الـOpenNext handler. **كل المدد 30 يوم** (`fbaea1f` — الاشتراك شهري) + stale-while-revalidate ساعة + stale-if-error يوم + المتصفح 5 دقايق. المفتاح = path من غير query. `revalidate` التفاصيل 3600.
- **حادثة Vary اتحلت (`4beba90`):** Cache API بتتجاهل Vary — طلبات RSC اتخزنت مكان HTML. الإصلاح: طلبات الراوتر بتعدّي من غير تخزين + المخزّن لازم text/html (والـAPIs application/json). **ممنوع الاعتماد على Vary في كاش Workers أبدًا.**
- **لغز الـ4 ساعات اتحل (`3b7bb3d`):** Browser Cache TTL العام → Respect Existing Headers + حذف القواعد المتداخلة. **ممنوع أي Browser TTL override جديد في الداشبورد.**
- **مقبرة الـKV:** 8,490 مفتاح قمامة — اتبدأ حذف 1,000/تشغيلة (`scripts/cleanup-kv-graveyard.js` + `kv-cleanup.yml` — الـcron من main بس).
- **CSP Report-Only (`fbaea1f`):** في next.config.ts — مصادر Adsterra + TMDB. مراقبة فقط.
- **بعد أي `build:cloudflare` محلي: امسح `.open-next` قبل `npx tsc`** (أخطاء وهمية من الباندل المولّد).

---

## 1) المشروع والبيئة

- **الموقع:** 4cima.com — منصة أفلام ومسلسلات (Next.js App Router على Cloudflare Workers + D1)
- **الفرع:** `main` هو الأساسي (اتوحّد مع `cloudflare-migration` — الاتنين ينبسوا في deploy.yml) | **النشر:** أي `git push` → GitHub Actions يبني وينشر + يمسح الكاش تلقائيًا
- **القاعدة الإنتاجية:** D1 `b50ec43e-...` (1.1GB، 49 جدول)
- **سيرفر التجربة:** منفذ 3000
- **قواعد ثابتة:** ممنوع commit/push/deploy بدون أمر صريح من اسلام • ممنوع لمس: `src/lib/db.ts` / `src/app/page.tsx` الرئيسية / Navbar / player-worker / ads.txt / package.json / .env • التقارير بصيغة STOP (A,B,C...) • الردود بالعربي المصري وبساطة (اسلام صفر برمجة)
- **الهدف العام:** فاتورة Cloudflare $0 مع زيادة الزوار 10-15× — D1 rows read كان 19.7B من 25B المجانية قبل تحسينات 18/9

## 2) آخر الحالة المنشورة (كلها لايف ومتحقق منها)

| Commit | الموضوع |
|---|---|
| `afa3528`/`3b6e74b` | استعلامات العربي IFNULL (رصد 80K صف/نداء) — خطة مثبتة على idx_*_original_lang |
| `0142b90` + `afc9e16` + `fa2a77c` | التنظيف الشامل: 130 ملف و~16K سطر قمامة من الريبو |
| `b54298c` | قوائم الأنواع دخلت سلسلة المزامنة (خطوة 4) |
| `2b39ae0` | صفحات التصنيفات أول صفحة partition+probe (409K→75 صف) |
| `a67f320` | ترجمة 47 مسلسل صيني بـllama-3.3-70b (OpenRouter) |
| `3eb230b` | إصلاح similar: 145,068 → 14 صف/نداء |
| `a250bc6` | مسح كاش إلزامي بعد كل نشر في deploy.yml |
| `ce5ece5` → `1e252ad`+`20cb251` | (17/9) sitemap + فهرس التصنيفات + أيتام + سرعة + أتمتة |

## 3) سلسلة ما بعد المزامنة (تلقائية داخل `3-sync-to-d1.js`)

بعد نجاح أي مزامنة، بالترتيب: **قوائم البحث القصيرة → مسح كاش الحافة → فهرس التصنيفات → قوائم الأنواع (`6-precompute-genre-lists` — بياخد ~35-40 دقيقة) → sitemap_urls**.
- الفشل في أي خطوة = تحذير في اللوج فقط (المزامنة نجحت أصلًا).
- أوضاع المزامنة: عادية / `--limit=` / `--tmdb-id=` / `--ids-file=` • `--rebuild-short-titles` مستقل • `--all` معطّل.

## 4) قواعد معمارية مهمة (لا تُخترق)

- **صيغة فلتر الحالة إلزامية:** `IFNULL(filter_status,'clean') IN ('clean','reviewed_approved')` — **ممنوع** `(X IS NULL OR X IN ...)` على أي استعلام D1: بتفتح MULTI-INDEX OR على فهرس filter (اترصد 3 مرات: similar 145K، التصنيفات 409K، العربي 80K). الاستثناء الوحيد: الـOR جوه probe بعد فهرس قائد (تفاصيل slug / anti-join) — متقاسة وسليمة.
- **مصدر صفحات الأنواع أول صفحة = `movies_by_genre`/`series_by_genre`** (partition + PK probe) — **مش `list_*_genre`** (ترتيب «التصنيف الأساسي أولًا + سقف 500» بيغير المحتوى عن الحي).
- **ممنوع `NOT IN (SELECT...)` و `json_each` مسح-كتالوج على الـhot path** — anti-join أو الجداول المجمّعة.
- **فحص الكود اليتيم لازم يغطي side-effect imports** (`import './x'`) — مش الاستيراد المسماي بس.
- `loading.tsx` الجذري **محذوف عمدًا** (حل soft-404) — **ممنوع إرجاعه نهائيًا**.
- جداول الفهرس = لقطة: أي تعديل على `buildTvGenreClause` في `genre-siblings.ts` يستلزم `node scripts/build-genre-index.js` (وقواعد تفريق TV الخمسة معلنة في `TV_DISAMBIGUATED_GENRES`).
- **كاش الحافة (`edge-cache-worker.ts`):** allowlist بس — ممنوع تكاش /admin • /api/ads • /api/continue-watching • /search • /profile والـAPIs غير المعلنة • أول طلب بعد المسح MISS — استنى ثواني قبل أي حكم.
- **ممنوع أي Browser TTL override في قواعد الداشبورد** + ممنوع الاعتماد على Vary في كاش Workers.
- **`meta.changes` في D1 بيرجع أرقام مشله للـUPDATE** — التحقق دايمًا بـSELECT بعد الكتابة.
- التوكنات في `.env.local`: الـpurge بـ`CF_CACHE_PURGE_TOKEN` (+`CF_ZONE_ID`) • D1 بـ`CLOUDFLARE_D1_TOKEN` • الترجمة عبر **OpenRouter** (`OPENROUTER_API_KEY_1/2/3`).
- بعد أي `build:cloudflare` محلي: امسح `.open-next` قبل `npx tsc`.

## 5) المفتوح / المؤجل (بترتيب القيمة)

1. **⚠️ مفتاح `GROQ_API_KEY` ملغي (401)** — إسلام يستبدله أو يحذفه من `.env.local` + `.env.local.example`. البديل الفعلي شغال: OpenRouter بنفس الموديل. بعد الحذف: ممكن تشيل الاسم من قايمة `verify-build-secrets.js` (دلوقتي مسابه عمدًا كمراقبة).
2. **⚠️ فصل تكامل Workers Builds من داشبورد كلاود فلير** (Settings → Build → Disconnect) — مسار موازٍ فاشل ومن غير مسح كاش. النشر الرسمي = GitHub Actions.
3. **مراقبة النتائج** (أسبوعين-٤ أسابيع): منحنى D1 rows_read بعد كاش الحافة + كل الإصلاحات (كان 417-757M/يوم) • جوجل كونسول: المعلقة 87,170 • CWV يتحسن تدريجيًا. ملاحظة: التوكن المحلي مالوش صلاحية GraphQL Analytics — المتابعة من داشبورد اسلام بس.
4. **الإعلانات:** Adsterra بيرجع 500 (من سيرفرهم) — متابعة مع الشركة. أول ما تستقر → CSP/COOP مع اختبار كامل.
5. **صيانة الـKV:** متبقي ~7,780 مفتاح قمامة (1,000/تشغيلة) — شغّل `node scripts/cleanup-kv-graveyard.js` محليًا أو الـworkflow.
6. **اختياري:** `name_en` للـ47 مسلسل صيني لسه CJK (سطر فرعي في البطاقات) — لو حابب يتظبط. مؤجل عام: CSS critical • keyset pagination • WebP • تدوير التوكنات (قرار إداري).

## 6) فهرس الأوامر السريعة

```bash
node scripts/3-sync-to-d1.js            # مزامنة محتوى + السلسلة التلقائية كاملة (5 خطوات)
node scripts/build-genre-index.js       # إعادة بناء فهرس التصنيفات (*_by_genre)
node scripts/6-precompute-genre-lists.js  # إعادة بناء list_*_genre (يدوي لو لزم — 35-40 دقيقة)
node scripts/build-sitemap-urls.js --remote --apply   # إعادة بناء الخريطة
npx tsc --noEmit                        # فحص الأنواع (بعد build محلي: امسح .open-next الأول)
curl -s -D - -o /dev/null "https://4cima.com/movies/SLUG" | grep -i x-edge-cache   # الكاش HIT؟
node scripts/verify-build-secrets.js    # فحص تسرب الأسرار للبناء (قبل أي نشر يدوي)
```
