# ♿ مشاكل إمكانية الوصول — محدّث بإعادة تدقيق 13/09/2026

> **إعادة تدقيق:** الملاحظات في نهاية كل بند تعكس الحالة الفعلية بعد النشر (commit `469c38d`).

## 6.1 — lang attribute ثابت
**الملف:** `src/app/layout.tsx:117`

```html
<html lang="ar" dir="rtl" data-scroll-behavior="smooth">
```

**المشكلة:** ثابت دائماً — حتى عندما يكون `useLang` = 'en'
**الحالة النهائية:** ⛔ **قرار مبرر — لا يُنفذ** (موقع عربي RTL فقط). تفعيل `dir` ديناميكي سيقلب التخطيط بالكامل. عالَج في التقرير كموصية خطرة (10.6).

---

## 6.2 — Skip Navigation
**الملف:** `src/components/common/SkipLink.tsx`

- موجود لكنه لا يظهر إلا عند focus — سلوك قياسي مقبول (معيار الويب نفسه).

**إعادة تدقيق:** ⚠️ منخفض الأولوية — سلوك الظهور عند focus صحيح أصولاً.

---

## 6.3 — Buttons بدون aria-label
- زر البحث — لا aria-label
- زر تبديل اللغة — لا aria-label
- MovieCard — Link لا يحتوي aria-label يصف الفيلم

**إعادة تدقيق:** ✅ نقطة إيجابية جديدة: مكون اللوجو الجديد `SiteLogo.tsx:88` يحتوي `aria-label="4cima — الرئيسية"` وعناصر زخرفية معلَّمة `aria-hidden="true"` — أفضل من النسخة القديمة.

---

## 6.4 — Images بدون alt text كافٍ
**الملف:** `src/components/common/TmdbImage.tsx`

- بعض الصور تستخدم alt فارغ أو generic.

**إعادة تدقيق:** ⚠️ ما زالت قائمة — تحتاج مسح شامل لـ alt في MovieCard/MegaHero. آمن للتنفيذ لكنه يتطلب مراجعة نصية.

---

## 6.5 — عدم وجود focus management
- عند فتح sidebar — focus لا ينتقل إليه
- عند إغلاق modal — focus لا يعود لزر الفتح

**إعادة تدقيق:** ⚠️ ما زالت قائمة. تحسين آمن لكنه غير حرج.

---

## 6.6 — Color Contrast
- بعض النصوص على خلفيات داكنة قد لا تفي بمعايير WCAG AA
- مثال: `text-slate-400` على `bg-slate-900`

**الحالة النهائية:** ⛔ **قرار تصميمي — لا يُنفذ آلياً.** مذكور في قائمة الخطرة (10.7).
