/**
 * المنطق الموحد لكل صفحات القوائم في الموقع
 * (الرئيسية للأقسام، صفحات اللغة، صفحات التصنيفات، الأنمي)
 *
 * القاعدة:
 * - كل صفحة تعرض 20 عملًا في الدفعة الأولى (SSR) و20 عملًا مع كل سكرول.
 * - السكرول اللانهائي موحّد: IntersectionObserver بجذر 400px.
 * - خريطة الإعلانات موحّدة في كل صفحات القوائم:
 *   1: 728×90 هيدر | 2: 300×250 عمود جانبي | 3: 160×600 سكرايبر ديسكتوب
 *   4: 468×60 فاصل قبل الفوتر | 5: 160×300 كارت داخل الشبكة (كل 25 كارت)
 *   6: 320×50 شريط الموبايل الثابت
 * - كل صفحات القوائم تنتهي بـ Footer.
 */

/** عدد الأعمال في الدفعة الواحدة (SSR + كل تحميل بالسكرول) — موحّد في الموقع كله */
export const LISTING_PAGE_SIZE = 20

/**
 * عدد الكروت في «البلوك العلوي» بجانب عمود الإعلانات الجانبي (ديسكتوب فقط).
 *
 * الحساب (قرار صاحب الموقع: البلوك ≈ ارتفاع عمود الإعلانات المحجوز — لا يُصغَّر):
 * - عمود الإعلانات: 300×250 (250px + إطار ≈ 11px) + gap 24px + 160×600 (600px + إطار ≈ 11px) ≈ 896px.
 * - ارتفاع الكارت = 1.5 × العرض (بوستر aspect-[2/3]) + 52px (بلوك النص h-[52px] في MovieCard).
 * - أعمدة grid-responsive بجانب عمود 300px: lg=6، xl=7، 2xl=8.
 * - 16 = صفان × 8 أعمدة عند 2xl (الشاشة التي ظهرت فيها 4 كروت يتيمة مع القيمة السابقة).
 *   عند 2xl: الكارت ≈ 172×311 (عند 1920px) → صفان + gap ≈ 638px، وعند أقصى عرض (2400px)
 *   الكارت ≈ 232×401 → صفان ≈ 817px — كلاهما < 896px → البلوك = ارتفاع عمود الإعلان
 *   بالضبط ولا يمتد عنه أبدًا، وصفر قصّ كروت، ولا كروت يتيمة ناقصة الصف.
 * - الدفعة الأولى 20 عملًا (LISTING_PAGE_SIZE) > 16 → restItems = 4 كروت
 *   في شبكة كاملة العرض من أول رسمة، ولا فرق في الـDOM لحظة الترطيب (hydration).
 * - نفس الثابت يحدد عدد صور eager (الثنية الأولى) في صفحات القوائم.
 * الجوال: كل الكروت تبقى في شبكة واحدة كاملة العرض (كما هي).
 */
export const LISTING_TOP_CARDS_COUNT = 16
