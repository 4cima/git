/**
 * src/lib/supabase.ts — DEPRECATED STUB
 * All auth and user data now live in D1 via auth-server.ts.
 * This stub exists only to catch leftover imports visually during build.
 */
export const supabase: any = null;
export async function getProfile(): Promise<null> { return null; }
