import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'حقوق النشر والملكية الفكرية' }

export default function CopyrightLayout({ children }: { children: React.ReactNode }) { return children }