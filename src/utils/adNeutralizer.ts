export const adNeutralizer = {
  init: () => {
    // Ad neutralizer initialization
  },
  cleanup: () => {
    // Ad neutralizer cleanup
  },
  injectIntoIframe: (iframe: HTMLIFrameElement | null) => {
    // Inject ad neutralizer into iframe
  },
}
